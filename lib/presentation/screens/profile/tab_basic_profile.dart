import 'package:flutter/material.dart';

class TabBasicProfile extends StatelessWidget {
  const TabBasicProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
